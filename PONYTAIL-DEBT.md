# Ponytail Debt Ledger

Generated by `/ponytail:ponytail-debt` — a snapshot of every deliberate
`ponytail:` shortcut in the repo, so a deferral doesn't quietly become
permanent. Re-run the scan periodically; update this file by hand when an
item is resolved or a new marker is added.

`grep -rnE '(#|//) ?ponytail:' .`

## Ledger

- `lib/sync.ts:54` — settings are not exported yet (always empty).
  **ceiling:** `exportData()` can't round-trip settings.
  **upgrade:** add `getAllSettings()` to `lib/db` when settings need to
  round-trip.

- `lib/cloud/activeProvider.ts:3` — active cloud provider held as a
  session-scoped in-memory singleton (no token persistence).
  **ceiling:** session ends or token expires → user must re-login.
  **upgrade:** add a `sessionStorage` backup in this file if persistence
  is ever needed.

**2 markers, 0 with no trigger.**

## Note

`docs/superpowers/plans/2026-06-24-dropbox-auto-sync.md:374` also contains the
same `ponytail:` text as `activeProvider.ts:3` — it's the implementation plan
that introduced that marker, quoted verbatim in a code block, not a second
independent shortcut. Not counted above.
