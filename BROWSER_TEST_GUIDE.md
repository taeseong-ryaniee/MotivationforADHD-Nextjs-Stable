# 브라우저 수동 테스트 가이드

개발 서버가 http://localhost:3000 에서 실행 중입니다.
브라우저에서 다음 항목들을 직접 확인해주세요.

---

## 🔧 사전 준비

1. **Chrome 브라우저 열기**
2. **DevTools 열기** (`F12` 또는 `Cmd/Ctrl + Shift + I`)
3. **http://localhost:3000** 접속

---

## 📋 Phase 1: 보안 & 안정성 테스트

### 1.1 Service Worker 등록 확인 ⭐⭐⭐⭐⭐

**위치**: DevTools > Application 탭 > Service Workers

**확인 사항**:
- [ ] Service Worker가 "activated and running" 상태
- [ ] Scope: `/`
- [ ] Source: `/sw.js`

**추가 확인**:
```
DevTools > Console에서 실행:
navigator.serviceWorker.getRegistration().then(reg => console.log('✓ Service Worker:', reg))
```

예상 결과: ServiceWorkerRegistration 객체 출력


### 1.2 XSS 방지 검증 ⭐⭐⭐⭐⭐

**테스트 순서**:
1. 메인 페이지에서 "오늘의 특별 일정" 입력창 찾기
2. 다음 악성 코드 입력 시도:
   ```
   <script>alert('XSS')</script>
   ```
3. "오늘 하루 시작!" 버튼 클릭

**예상 결과**:
- [ ] alert 창이 **나타나지 않음** (XSS 차단됨)
- [ ] Toast 알림으로 "유효하지 않은 문자가 포함되어 있습니다" 표시
- [ ] Console에 Zod validation 에러 로그


### 1.3 타입 안전성 확인 ⭐⭐⭐⭐⭐

**위치**: DevTools > Console

**확인 사항**:
- [ ] TypeScript 컴파일 에러 없음 (빌드 성공)
- [ ] Console에 런타임 에러 없음
- [ ] 페이지 로드 시 에러 없이 정상 표시

---

## 📋 Phase 2: 성능 & UX 테스트

### 2.1 Toast 알림 시스템 ⭐⭐⭐⭐⭐

**테스트 1: 성공 메시지**
1. "오늘 하루 시작!" 버튼 클릭
2. To-do 생성 완료 후 "오늘 생성한 To-do 보기" 클릭
3. "클립보드에 복사하기" 버튼 클릭

**예상 결과**:
- [ ] 화면 상단 중앙에 Toast 알림 표시
- [ ] "클립보드에 복사되었습니다!" 메시지
- [ ] 3초 후 자동으로 사라짐
- [ ] alert() 창이 **나타나지 않음**

**테스트 2: 에러 메시지**
1. 개발자 도구에서 네트워크 오프라인 설정
2. "오늘 하루 시작!" 버튼 클릭

**예상 결과**:
- [ ] Toast 에러 알림 표시
- [ ] 빨간색 배경의 에러 메시지


### 2.2 에러 바운더리 ⭐⭐⭐⭐⭐

**테스트 방법**:
1. DevTools > Console에서 다음 실행:
   ```javascript
   throw new Error('Test Error')
   ```

**예상 결과**:
- [ ] 앱이 완전히 크래시되지 않음
- [ ] 에러 페이지 표시 (개발 모드에서 상세 정보 포함)
- [ ] "다시 시도" 버튼 표시


### 2.3 로딩 상태 ⭐⭐⭐⭐⭐

**테스트 위치**:
1. 메인 페이지 > "오늘 하루 시작!" 버튼 클릭 시
2. `/history` 페이지 접속 시
3. `/todo/[id]` 페이지 접속 시

**예상 결과**:
- [ ] 로딩 중 스피너 애니메이션 표시
- [ ] 버튼에 "To-do 생성 중..." 텍스트
- [ ] aria-busy 상태 변경 (스크린 리더 확인)


### 2.4 React.memo 성능 검증 ⭐⭐⭐⭐

**테스트 방법**:
1. React DevTools 설치 (Chrome 확장)
2. DevTools > Profiler 탭 > 녹화 시작
3. 특별 일정 입력창에 텍스트 입력
4. 녹화 종료 후 렌더링 분석

**예상 결과**:
- [ ] `MotivationCard` 컴포넌트가 불필요하게 재렌더링되지 않음
- [ ] `BaseButton` 컴포넌트가 불필요하게 재렌더링되지 않음


### 2.5 IndexedDB v2 스키마 ⭐⭐⭐⭐⭐

**위치**: DevTools > Application 탭 > IndexedDB > MotivationForADHD

**확인 사항**:
- [ ] 데이터베이스 버전: **2**
- [ ] `todos` 테이블 인덱스:
  - `id` (primary key)
  - `date`
  - `createdAt`
  - `[date+createdAt]` (복합 인덱스)


### 2.6 TanStack Query 캐싱 ⭐⭐⭐⭐

**테스트 방법**:
1. DevTools > Network 탭 열기
2. 페이지 새로고침 (F5)
3. 다른 페이지 이동 후 메인 페이지로 돌아오기

**예상 결과**:
- [ ] 첫 로드 시 `/api/content/ko` 요청 1회
- [ ] 1시간 내 재방문 시 추가 요청 없음 (캐시 사용)
- [ ] Network 탭에서 "(from cache)" 표시

---

## 📋 Phase 3: 접근성 테스트

### 3.1 ARIA 라벨 확인 ⭐⭐⭐⭐⭐

**테스트 방법**:
1. DevTools > Elements 탭
2. 다음 요소들 검사:

**확인 항목**:
- [ ] Skip link: `href="#main-content"`
- [ ] Main 랜드마크: `<main id="main-content" role="main">`
- [ ] 특별 일정 입력: `aria-label="오늘의 특별 일정"`
- [ ] 생성 버튼: `aria-busy={isCreating}`, `aria-label`
- [ ] To-do 섹션: `aria-live="polite"`, `aria-atomic="true"`
- [ ] 복사 버튼: `aria-label="To-do 전체 내용 클립보드에 복사"`


### 3.2 키보드 네비게이션 ⭐⭐⭐⭐⭐

**테스트 1: Tab 키 네비게이션**
1. 페이지 로드 후 `Tab` 키를 계속 누르기
2. 모든 인터랙티브 요소를 순차적으로 탐색

**예상 결과**:
- [ ] 첫 Tab: Skip link 포커스 (화면에 표시됨)
- [ ] 각 요소에 파란색 포커스 아웃라인 (2px solid)
- [ ] 포커스 순서가 논리적 (위→아래, 왼쪽→오른쪽)
- [ ] 모든 버튼과 링크가 포커스 가능

**테스트 2: 키보드 단축키**
1. 메인 페이지에서 `Ctrl/Cmd + Enter` 키 누르기

**예상 결과**:
- [ ] "오늘 하루 시작!" 버튼 클릭과 동일한 동작
- [ ] To-do 생성 시작
- [ ] 하단에 단축키 힌트 표시


### 3.3 Skip Link ⭐⭐⭐⭐⭐

**테스트 방법**:
1. 페이지 로드 후 `Tab` 키 한 번 누르기
2. Skip link에 포커스되면 `Enter` 키 누르기

**예상 결과**:
- [ ] Skip link가 화면에 표시됨
- [ ] Enter 시 메인 콘텐츠로 포커스 이동
- [ ] 네비게이션 건너뛰기 가능


### 3.4 색상 대비 (WCAG AA) ⭐⭐⭐⭐⭐

**테스트 방법**:
1. DevTools > Rendering 탭 > "Emulate vision deficiencies" 활성화
2. 다크 모드 토글 (시스템 설정 또는 확장 프로그램)

**Light Mode 확인**:
- [ ] 주요 텍스트(primary) / 배경: 17:1 이상
- [ ] 보조 텍스트(secondary) / 배경: 4.5:1 이상

**Dark Mode 확인**:
- [ ] 주요 텍스트(primary) / 배경: 17:1 이상
- [ ] 보조 텍스트(secondary) / 배경: 4.5:1 이상
- [ ] 모든 텍스트가 명확하게 읽힘


### 3.5 스크린 리더 테스트 (선택사항) ⭐⭐⭐⭐

**Mac**: VoiceOver (`Cmd + F5`)
**Windows**: NVDA (무료) 또는 JAWS

**테스트 시나리오**:
1. 스크린 리더 활성화
2. 페이지 탐색

**예상 결과**:
- [ ] 모든 버튼과 링크의 목적이 명확하게 읽힘
- [ ] 이미지와 아이콘에 대체 텍스트 제공
- [ ] aria-live 영역의 변경사항 자동 알림
- [ ] 장식적 아이콘은 읽히지 않음 (aria-hidden)

---

## 📋 PWA 기능 테스트

### 4.1 오프라인 동작 ⭐⭐⭐⭐⭐

**테스트 방법**:
1. 페이지 완전히 로드
2. DevTools > Network 탭 > "Offline" 체크
3. 페이지 새로고침

**예상 결과**:
- [ ] 오프라인 페이지 표시 (캐시에서 로드)
- [ ] 기본 UI 요소 정상 표시
- [ ] Service Worker가 캐시된 리소스 제공


### 4.2 홈 화면에 추가 ⭐⭐⭐⭐⭐

**테스트 방법** (모바일 또는 DevTools 에뮬레이션):
1. Chrome 메뉴 > "홈 화면에 추가" 또는 "설치"
2. 설치 프롬프트 확인

**예상 결과**:
- [ ] 설치 가능 (PWA 기준 충족)
- [ ] 앱 이름: "산만이의 아침"
- [ ] 앱 아이콘 표시
- [ ] 독립 실행형 앱으로 실행


### 4.3 Manifest 파일 ⭐⭐⭐⭐⭐

**위치**: DevTools > Application 탭 > Manifest

**확인 사항**:
- [ ] Name: "산만이의 아침"
- [ ] Short name: "산만이"
- [ ] Description: ADHD 사용자를 위한...
- [ ] Icons: 192x192, 512x512
- [ ] Theme color: `#2563eb`
- [ ] Display: standalone
- [ ] Start URL: `/`

---

## 📊 Chrome DevTools Lighthouse 검사

### 실행 방법

1. DevTools > Lighthouse 탭
2. 카테고리 선택:
   - ✅ Performance
   - ✅ Accessibility
   - ✅ Best Practices
   - ✅ PWA
3. "Analyze page load" 클릭

### 예상 점수 (목표)

| 카테고리 | 목표 점수 | 중요 지표 |
|---------|----------|----------|
| **Performance** | 90+ | FCP, LCP, TBT |
| **Accessibility** | 95+ | ARIA, 색상 대비, 포커스 |
| **Best Practices** | 95+ | HTTPS, 보안 헤더 |
| **PWA** | 90+ | Service Worker, Manifest |

### 주요 확인 항목

**Performance**:
- [ ] First Contentful Paint (FCP) < 1.8s
- [ ] Largest Contentful Paint (LCP) < 2.5s
- [ ] Total Blocking Time (TBT) < 200ms
- [ ] Cumulative Layout Shift (CLS) < 0.1

**Accessibility**:
- [ ] 모든 버튼에 접근 가능한 이름
- [ ] 충분한 색상 대비
- [ ] Skip link 구현
- [ ] ARIA 속성 올바르게 사용

**PWA**:
- [ ] Service Worker 등록됨
- [ ] 오프라인에서 작동
- [ ] 200 응답 코드
- [ ] Manifest 유효

---

## ✅ 테스트 체크리스트 요약

완료한 항목에 체크하세요:

### Phase 1 (보안 & 안정성)
- [ ] Service Worker 활성화
- [ ] XSS 방지 작동
- [ ] 런타임 에러 없음

### Phase 2 (성능 & UX)
- [ ] Toast 알림 작동
- [ ] 에러 바운더리 작동
- [ ] 로딩 스피너 표시
- [ ] IndexedDB v2 확인
- [ ] TanStack Query 캐싱

### Phase 3 (접근성)
- [ ] ARIA 라벨 완전 적용
- [ ] 키보드 네비게이션 작동
- [ ] 색상 대비 충족
- [ ] Skip link 작동

### PWA
- [ ] 오프라인 동작
- [ ] 홈 화면 추가 가능
- [ ] Manifest 유효

### Lighthouse
- [ ] Performance 90+
- [ ] Accessibility 95+
- [ ] Best Practices 95+
- [ ] PWA 90+

---

## 🐛 문제 발견 시

문제를 발견하면 다음 정보를 기록해주세요:

1. **문제 설명**: 무엇이 예상과 다른가?
2. **재현 단계**: 어떻게 하면 문제가 발생하는가?
3. **브라우저 정보**: Chrome 버전, OS
4. **Console 에러**: DevTools Console에 표시된 에러
5. **스크린샷**: 문제 상황 캡처

---

## 📝 테스트 완료 후

모든 테스트를 완료하면:
1. 체크리스트 스크린샷 저장
2. Lighthouse 보고서 저장 (JSON 또는 HTML)
3. 발견된 이슈 정리

**개발 서버 종료**: `Ctrl + C` (터미널에서)
